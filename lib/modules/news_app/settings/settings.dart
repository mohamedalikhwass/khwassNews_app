import 'package:flutter/material.dart';

class NewsAppSettings extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Text(
        'Settings'
    );
  }
}
